# NuExtract use branch:

Branch born because Opendataloader fails to pick up some pieces of text and we will use NuExtract to get the text in markdown first.

Changing the text parser to NuExtract. Noiw we ned to have NuExtract text -> json parsing.